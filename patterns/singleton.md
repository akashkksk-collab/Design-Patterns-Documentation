# Singleton Pattern

## Problem

An application may require only one instance of a particular object.

## Solution

Singleton ensures that only one instance is created and shared.

## Example

Application configuration manager.

## When to Use

Use Singleton when one shared instance is required.

## Key Idea

One class → One shared instance
